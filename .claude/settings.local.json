{
  "permissions": {
    "allow": [
      "Bash(npx tsc --noEmit)",
      "Bash(xargs grep -l \"authenticate\\\\|authorization\\\\|protected\")",
      "Bash(grep -n \"process.env\" src/**/*.ts)"
    ]
  }
}
